//commands start with ':' and then the git command you want to exec is on the following line
:all
git add -A && git commit
:lazy
git add -A && git commit && git push
:yank
git stash && git pull
:shove
git push
